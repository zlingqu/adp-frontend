import Vue from 'vue';
import Vuex from 'vuex';

// modules
import Passwd from './module/passwd';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    //
  },
  mutations: {
    //
  },
  actions: {
    //
  },
  modules: {
    Passwd,
  },
});
